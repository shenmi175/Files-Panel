services:
  panel:
    build: .
    container_name: files-panel
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DATA_DIR: /data
    volumes:
      - ./data:/data
      - ${HOME}/.ssh:/root/.ssh:ro
